import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
  },
  headerText: {
    color: '#ff8d01',
    fontWeight: 'bold',
    fontSize: 40,
    marginBottom: 24,
    marginTop: 8,
    textAlign: 'center',
  },
});
